services:
  - type: web
    name: market-material-search
    env: python
    runtime: python3.13
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    envVars:
      - key: LEXIANG_AUTH_TOKEN
        sync: false
